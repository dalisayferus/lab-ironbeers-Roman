    <div id='beers-container'></div>
    {{#if this.image_url}}
      <img src='{{this.image_url}}' alt='{{this.tagline}}'/>
    {{else}}
    <div id="no-pic">
      <img src='/images/no-image-icon-23483.png'/>
      </div>
    {{/if}}
    <br />
    <h3>{{this.name}}</h3>
    <ul>
      <li><p>{{this.description}}</p></li>
      <li><p>{{this.tagline}}</p></li>
    </ul>
    </div>